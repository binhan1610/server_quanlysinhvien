package com.example.demodo.repository;

import com.example.demodo.entity.Monhocs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MonhocsRepository extends JpaRepository<Monhocs, Integer> {
}